export const ROUTES = {
    home: "/home",

}

export const PRICE = {
    min: 0,
    max: 1000000000
}

const API_HOST = "localhost:3003"
export const API_URL = {
    PRODUCT: {
        HOME_PRODUCT: `${API_HOST}/products/home_product`
    }
}